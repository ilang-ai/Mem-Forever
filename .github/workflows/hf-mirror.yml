name: Mirror to HuggingFace

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  mirror:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Push to HuggingFace
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: |
          git remote add hf https://i-Lang:${HF_TOKEN}@huggingface.co/datasets/i-Lang/Mem-Forever || true
          git push hf main --force
